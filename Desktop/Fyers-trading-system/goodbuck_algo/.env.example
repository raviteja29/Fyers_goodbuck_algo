# Frontend Environment Example

# Base URL for backend API (keep name consistent across docs)
VITE_API_URL=http://localhost:3001/api

# (Optional) Public Fyers config values - DO NOT expose secrets
# Only include if the frontend needs to display or construct auth URLs dynamically
# VITE_FYERS_APP_ID=your_fyers_app_id
# VITE_FYERS_REDIRECT_URI=http://localhost:5173

# Add additional VITE_ prefixed variables here as needed